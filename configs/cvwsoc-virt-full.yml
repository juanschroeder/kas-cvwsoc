header:
  version: 12
  includes:
    - repos.yml
target:
  - cvwsoc-image-doom
local_conf_header:
  common: |
    MACHINE = "cvwsoc-virt"
    DISTRO = "distro-cvwsoc-basic"
    # Uncompressed Kernel in the Image is faster for simulation
    KERNEL_IMAGETYPE = "Image"

